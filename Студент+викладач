SELECT DISTINCT subjects.name
FROM grades
JOIN subjects ON grades.subject_id = subjects.id
JOIN teachers ON subjects.teacher_id = teachers.id
WHERE grades.student_id = 1 AND teachers.name = 'John Doe';
